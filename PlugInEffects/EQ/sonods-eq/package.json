{
  "name": "sonods-eq-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:dsp": "cd packages/dsp-core && wasm-pack build --target no-modules --release && cd ../eq-engine && node scripts/copy-wasm.mjs",
    "test:dsp": "cd packages/dsp-core && cargo test",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @sonods/demo dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
